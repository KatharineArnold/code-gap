{{!-- <link href="https://stackpath.bootstrapcdn.com/bootswatch/4.1.3/flatly/bootstrap.min.css" rel="stylesheet" integrity="sha384-gJWVjz180MvwCrGGkC4xE5FjhWkTxHIR/+GgT8j2B3KKMgh6waEjPgzzh7lL7JZT"
    crossorigin="anonymous"> --}}

<div class="container userContainer">
    <div class="row">
        <div class="col">
            <h1 class="userTitle"> Welcome {{user.name}}</h1>
        </div>
    </div>

    <div class="row">
        {{!-- can we add if nonProfitProfile too?? --}}
        <div class="col-md-4 offset-md-4">
            <div class="card mb-3">
                <div class="card-body">
                    <img style="height: 100%; width: 100%; display: block;" src="{{user.image}}" alt="Card image">
                </div>
                <div class="card-body">
                    {{#if volunteerProfile}}
                        <p class="card-text">Email: {{user.email}}</p>
                        <p class="card-text">Location: {{volunteerProfile.location}}</p>
                        <p class="card-text">Availability: {{volunteerProfile.availability}} hours per week</p>
                        <a id="update-volunteer" href="volunteer/{{volunteerProfile.id}}/update" role="button" class="btn btn-primary">Update
                            Profile</a>
                        <button id="deleteButton" role="button" class="btn btn-success" data-volunteer_profile_id="{{volunteerProfile.id}}">
                            Delete Profile
                        </button>
                    {{else}}
                        <a id="createProfile" href="/volunteer/create" class="btn btn-primary">Create a Volunteer
                            Profile</a>
                    {{/if}}
                </div>
            </div>
        </div>

    </div>

    <div class="row">
        {{#if nonProfitProfiles}}
            <div class="col">
                <h3>Organization Memberships</h3>
                {{#each nonProfitProfiles}}
                    <p>{{companyName}}</p>
                    <p class="card-text">Description: {{companyDescription}}</p>
            </div>
    </div>
    {{/each}}

    {{else}}
        <a id="createNonProfitProfile" href="/non-profit/create" class="btn btn-primary">Create an
            Organization
            Profile</a>
        {{/if}}
</div>

<script src="/js/user.js"></script>
